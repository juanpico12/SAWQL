/**
 * * Enum de las URLs de la aplicación
 */
export enum ROUTES {
    LOGIN = '/login',
    FORGOT_PASSWORD='/login/forgot-password',
    VERIFIED_EMAIL='/login/verify-email-address',
    HOME = '/app/principal',
 }